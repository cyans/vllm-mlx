A short note with no Markdown headers at all. It's just a few lines of
prose that should still be indexable. The chunker treats the whole file
as a single section with empty header text.

Useful for testing the pre-header / no-header path of the chunker.
